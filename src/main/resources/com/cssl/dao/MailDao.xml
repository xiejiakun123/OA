<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cssl.dao.mail.MailDao">
    <select id="cxyx" resultType="Aoa_mail_reciver">
      SELECT *  FROM aoa_mail_reciver WHERE is_read=0 AND is_star=0 AND mail_reciver_id=#{uid}
    </select>
</mapper>