<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cssl.dao.notice.NoticeDao">
    <select id="cxjl" resultType="Aoa_notice_user_relation">
        SELECT  * FROM aoa_notice_user_relation WHERE  is_read=0 AND relatin_user_id=#{uid}
    </select>
</mapper>